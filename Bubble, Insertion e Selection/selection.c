#include <stdio.h>
#include <stdlib.h>

void selectionSort(int *array, int size){
    int i, j, greater;
    for (i = 0; i < size; i++){
        greater = array[i];
        for(j = i + 1; j < size; j++){
            if(greater > array[j]){
                gre
            }
        }
    }
}
void main(){

}
